# Neon Postgres connection string (with ?sslmode=require)
DATABASE_URL=

# Google Gemini API key — used by retrieval rerank + the agent/graph LLM
GOOGLE_GENERATIVE_AI_API_KEY=

# Optional: Langfuse tracing. Leave blank to disable.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
